This regression test checks the per-line maximum relative error field for FP64.
The same source line executes several times; the JSON keeps the last relative error and also reports the maximum seen on that line.
